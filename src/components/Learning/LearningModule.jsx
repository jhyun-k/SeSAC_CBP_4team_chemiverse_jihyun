import React from "react";

const LearningModule = ({ data }) => {
    return (
        <div>
            LearningModule
            <h1>{data.name}</h1>
        </div>
    );
};

export default LearningModule;
